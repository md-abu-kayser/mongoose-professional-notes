# Custom Connection Pool Management

`mongoose.createConnection(uri, { maxPoolSize: 10 })`. Separate connections per tenant or workload.

> 📘 Next: [Change Streams & Real‑Time Updates](04-change-streams-and-real-time-updates.md)
