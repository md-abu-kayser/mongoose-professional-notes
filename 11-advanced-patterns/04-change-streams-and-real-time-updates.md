# Change Streams & Real‑Time Updates

`Model.watch()` returns a change stream. Listen for `insert`, `update`, `delete` events. Pipe to WebSocket for live features.

> 📘 Next: [Hybrid Approaches with Raw MongoDB Driver](05-hybrid-approaches-with-raw-mongodb-driver.md)
