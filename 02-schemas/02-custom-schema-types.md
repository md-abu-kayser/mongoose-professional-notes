# Custom Schema Types

Extend `mongoose.SchemaType` to create custom types (e.g., `Int32`, `Email`). Not commonly needed.

> 📘 Next: [Schema Options – timestamps, versionKey, strict](03-schema-options-timestamps-versionkey-strict.md)
