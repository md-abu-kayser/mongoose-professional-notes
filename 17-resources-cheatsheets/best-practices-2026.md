# Best Practices 2025

- Use `async/await`, avoid callbacks.
- Set `strict: 'throw'` in development.
- Use connection pools appropriately.
- Integrate validation with schema.
- Write test with `mongodb-memory-server`.
